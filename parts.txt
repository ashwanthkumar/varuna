# Varuna — JLCPCB Bill of Materials
# Board: 200×100mm, 2-layer. All C# parts from jlcpcb.com / lcsc.com.

# ── Power supply ──────────────────────────────────────────────────────────────
U_PSU    HLK-5M05             C209907   AC-DC isolated module 230V→5V/1A (Hi-Link)
SW1      SS12D10G3            C7431053  SPDT slide switch, vertical, controller ON/OFF

# ── Mains protection (no PCB fuse — upstream 2A MCB in DB panel) ──────────────
RV1      TMOV14RP275E         C1528070  MOV varistor 275Vac, across L-N at AC terminal

# ── Motor control (PCB switches the external contactor COIL) ─────────────────
U_DRV     ULN2003ADR          C7512     Darlington array SOIC16 (GPIO→relay driver)
RLY_MOTOR SRD-05VDC-SL-C      C35449    5V SPDT relay — switches 230VAC contactor coil

# ── AC & coil connectors ──────────────────────────────────────────────────────
J_AC     WJ128V-5.0-3P        C8270     AC mains input: L / N / E (3-pole, bottom edge)
J_COIL   WJ500V-5.08-2P       C8465     Contactor coil out: A1 (switched Live) / A2 (Neutral)
J_OL     WJ500V-5.08-2P       C8465     Overload relay aux NC contact sense: OL / GND

# ── Energy metering (motor V/I/power; current-based dry-run) ─────────────────
U_MTR    BL0942               C2909509  Single-phase energy-metering IC (UART/SPI to ESP32 GPIO25/26)
U_VT     ZMPT101B             C111858   Voltage transformer (mains V sense, isolated)
J_CT     WJ500V-5.08-2P       C8465     External current-transformer input: CT1 / CT2 (bottom edge)
R_BUR    20R 0603 (REF)       —         CT burden resistor — SIZE to chosen CT ratio (datasheet)
R_VT1/2  100k 0805 x2 (REF)   —         ZMPT primary series Rs (2 in series for 230Vac rating)
R_VBUR   1k 0603 (REF)        —         ZMPT secondary burden to BL0942 VP
C_MTR    100nF 0603           —         BL0942 VDD decoupling
#  EXTERNAL (not on PCB): a clamp/loop CURRENT TRANSFORMER around the motor Live
#  wire; its 2 leads land on J_CT. Pick a CT (e.g. ZMCT103C or SCT-013) rated for
#  >=15A and set R_BUR for ~full-scale at the BL0942 input. ⚠ REFERENCE values —
#  finalize R_BUR / R_VT / R_VBUR and the BL0942 interface (UART vs SPI, SEL pin)
#  against the BL0942 + CT datasheets before fabrication.

# ── EXTERNAL DOL components (DIN-rail, NOT on PCB — install in enclosure) ─────
#  Contactor  : LEYDEN CJX2-3211, 32A AC-3, 220/230VAC 50Hz coil, 3-pole.
#               Coil A1/A2 → J_COIL. For single-phase motor, parallel two poles
#               for the Live conductor (1/L1+3/L2 → 2/T1+4/T2) for longer contact
#               life; route Neutral through the third pole or straight.
#  Overload   : MAGNUM MaK-1 2P, single-phase, 13-21A range. Clips under the
#               CJX2 contactor. Set dial to ~13A (motor FLA for 2HP single-phase).
#               95-96 NC aux contact → J_OL (trip sense to GPIO21).
#  MCB        : 20A Type-C in DB panel (upstream, handles motor inrush).
#  NOTE: contactor must be AC3 >= 15A. Do NOT use enclosed manual starters whose
#        internal contactor is only AC3 10A (e.g. MAGNUM MCHK-1) — undersized
#        for 15A/2HP, and their push-button latch logic conflicts with automation.

# ── Sensor inputs — 30-40m CAT6 UTP run from overhead tank ───────────────────
J_FL1    WJ500V-5.08-2P       C8465     Float switch 1: tank LOW  (bottom edge, CAT6 pair 1)
J_FL2    WJ500V-5.08-2P       C8465     Float switch 2: tank HIGH (bottom edge, CAT6 pair 2)
J_PR     DB128L-5.08-4P-GN-S  C2827883  SS probe 4P: PR1(LOW)/PR2(MID)/PR3(HIGH)/COM (CAT6 pair 3+4)

# ── Surge protection — TVS arrays for outdoor cable ──────────────────────────
TVS_FL   PRTR5V0U2X           C2827688  ESD/TVS for float lines FL1 + FL2
TVS_PR12 PRTR5V0U2X           C2827688  ESD/TVS for probe lines PR1 + PR2
TVS_PR3C PRTR5V0U2X           C2827688  ESD/TVS for probe line PR3 + excitation COM

# ── MCU (buy locally — not assembled by JLCPCB) ──────────────────────────────
ESP32    ESP32 DevKit V1       —         DOIT WROOM-32, 38-pin. Plugs into ESP_L/ESP_R headers.
ESP_L    Female header 19-pin  —         2.54mm pitch (left DevKit rail)
ESP_R    Female header 19-pin  —         2.54mm pitch (right DevKit rail)
J_DBG    S3B-XH-A             C160404   JST-XH 3-pin RIGHT-ANGLE (side-entry) UART debug:
#                                         TX(GPIO1)/RX(GPIO3)/GND. Latching+polarized; mating
#                                         lead plugs in from the bottom board edge / below the
#                                         enclosure. Serial logs via a 3V3 USB-TTL adapter
#                                         (flashing still via DevKit USB).

# ── LEDs & resistors ──────────────────────────────────────────────────────────
# LED_PWR  : hardware power indicator, always on when SW1 is on. No GPIO.
# All others: GPIO-controlled from ESP32.
LED_PWR  Red LED 0603          —         Power on indicator (5V rail, hardware — no GPIO)
R_PWR    1kΩ 0603              —         LED_PWR current limit (≈3.5mA at 5V)
LED1     Green LED 0603        C965799   WiFi/system status   → GPIO4
R_LED    330Ω 0603             —         LED1 current limit
LED_MOTOR Yellow LED 0603      —         Motor running        → GPIO17
R_MOTOR  330Ω 0603             —         LED_MOTOR current limit
LED_FL1  Blue LED 0603         —         Float switch 1 state → GPIO19
R_FL1    330Ω 0603             —         LED_FL1 current limit
LED_FL2  Blue LED 0603         —         Float switch 2 state → GPIO18
R_FL2    330Ω 0603             —         LED_FL2 current limit
LED_PRB  Blue LED 0603         —         Probe water detected → GPIO5
R_PRB    330Ω 0603             —         LED_PRB current limit

# ── Buttons ───────────────────────────────────────────────────────────────────
# Active-LOW momentary pushbuttons. Board has 10k pull-up + 100nF debounce per button.
BTN_PAIR TS-1101-C-W          C318938   6x6mm THT tactile, WiFi pairing/config (hold 3s) → GPIO0
BTN_RST  TS-1101-C-W          C318938   6x6mm THT tactile, factory reset (hold 5s) → GPIO16
#  Both labelled PAIR/RESET on silk, grouped below the HLK module. For finger access
#  through the transparent cover, fit a tall button cap/extender or a cover hole over
#  the 6x6 actuator (footprint is fixed; cap height is a purchasing choice).
#  NOTE: 6 LEDs are grouped in a labelled row (PWR/WIFI/MOTOR/FL1/FL2/PRB) at the top
#        edge, visible through the transparent front cover (no light pipes/wiring needed).
R_BPAIR  10kΩ 0603             —         BTN_PAIR pull-up
C_BPAIR  100nF 0603            —         BTN_PAIR debounce cap
R_BRST   10kΩ 0603             —         BTN_RST pull-up
C_BRST   100nF 0603            —         BTN_RST debounce cap

# ── PSU decoupling ────────────────────────────────────────────────────────────
C1       100µF 1206            —         PSU output bulk capacitor
C2       100nF 0603            —         PSU output decoupling capacitor

# ── Sensor front-end passives (×2 per float, ×3 per probe) ───────────────────
RU_FL1/2  10kΩ 0603 ×2        —         Float GPIO pull-up resistors
RS_FL1/2  220Ω 0603 ×2        —         Float series protection resistors
CF_FL1/2  100nF 0603 ×2       —         Float debounce capacitors
RD_PR1-3  10kΩ 0603 ×3        —         Probe GPIO pull-DOWN resistors
RS_PR1-3  220Ω 0603 ×3        —         Probe series protection resistors
CF_PR1-3  100nF 0603 ×3       —         Probe filter capacitors
R_EXC     1kΩ 0603 ×1         —         Probe excitation current limiter (GPIO33→COM)

# ── Not on PCB — install at site ─────────────────────────────────────────────
—        MCB 2A               —         DB panel upstream overcurrent protection
—        SS probes ×3 (type 304 or 316 SS rods, ~6mm dia)  — sump electrodes
—        Float switches ×2    —         Overhead tank HIGH and LOW sensors
—        CAT6 UTP cable       —         30-40m run, overhead tank to controller
